import {  RouterModule,Routes  } from '@angular/router';
import {  ModuleWithProviders  } from '@angular/core';

const routes:Routes=[];

export let SharedRouting : ModuleWithProviders =RouterModule.forChild(routes);